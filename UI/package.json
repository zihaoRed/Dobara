{
  "name": "dobara-demo",
  "private": true,
  "packageManager": "pnpm@9.15.9",
  "scripts": {
    "dev": "pnpm --parallel -r dev",
    "dev:consumer": "pnpm --filter @dobara/consumer dev",
    "dev:tablet": "pnpm --filter @dobara/store-tablet dev",
    "dev:management": "pnpm --filter @dobara/management dev",
    "dev:portal": "pnpm --filter @dobara/portal dev",
    "dev:device-check": "pnpm --filter @dobara/device-check dev",
    "build": "pnpm -r build",
    "build:preview": "pnpm build && mkdir -p dist && cp -r apps/portal/dist/* dist/ && mkdir -p dist/consumer && cp -r apps/consumer/dist/* dist/consumer/ && mkdir -p dist/tablet && cp -r apps/store-tablet/dist/* dist/tablet/ && mkdir -p dist/management && cp -r apps/management/dist/* dist/management/ && mkdir -p dist/device-check && cp -r apps/device-check/dist/* dist/device-check/",
    "lint": "pnpm -r lint"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  },
  "engines": {
    "node": ">=18"
  }
}
